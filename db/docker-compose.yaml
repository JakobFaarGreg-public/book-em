services:
  database:
    image: postgres:latest
    ports:
    - 5432:5432
    restart: always
    env_file:
      - .env
    volumes:
      - ./db-data/:/var/lib/postgresql/data/
      - ./init.sql:/docker-entrypoint-initdb.d/init.sql
